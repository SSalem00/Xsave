// popup.js — placeholder for future settings (e.g. quality picker, filename format)
document.addEventListener("DOMContentLoaded", () => {
  // Future: load saved settings from chrome.storage.sync
});
